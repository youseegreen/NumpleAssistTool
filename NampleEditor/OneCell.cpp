#include "OneCell.h"

int OneCell::cellSize = 100;
int OneCell::fontB = CreateFontToHandle(NULL, cellSize / 2, 1);
int OneCell::fontS = CreateFontToHandle(NULL, cellSize / 5, 1);
int OneCell::fontD = CreateFontToHandle(NULL, cellSize / 2, 5);